diff --git a/.github/scripts/poll-workflow.sh b/.github/scripts/poll-workflow.sh
new file mode 100644
index 0000000..9e4adb4
--- /dev/null
+++ b/.github/scripts/poll-workflow.sh
@@ -0,0 +1,40 @@
+#!/bin/bash
+
+WORKFLOW_NAME="$1"
+TAG_NAME="$2"
+GITHUB_REPOSITORY="$3"
+MAX_ATTEMPTS=30
+
+ATTEMPT=0
+while [ $ATTEMPT -lt $MAX_ATTEMPTS ]; do
+  RUNS=$(gh api -H "Accept: application/vnd.github+json" \
+    /repos/${GITHUB_REPOSITORY}/actions/runs \
+    -q ".workflow_runs[] | select(.name == \"$WORKFLOW_NAME\" and .event == \"repository_dispatch\" and .head_branch == \"sync/$TAG_NAME\") | [.id, .status, .conclusion] | @csv" | head -n 1)
+  
+  if [[ -z "$RUNS" ]]; then
+    echo "No workflow run found. Attempt $ATTEMPT of $MAX_ATTEMPTS"
+    ATTEMPT=$((ATTEMPT + 1))
+    sleep 10
+    continue
+  fi
+
+  IFS=',' read -r -a RUN_DATA <<< "$RUNS"
+  STATUS=${RUN_DATA[1]//\"/}
+  CONCLUSION=${RUN_DATA[2]//\"/}
+
+  echo "Current status: $STATUS, conclusion: $CONCLUSION"
+
+  if [[ "$STATUS" == "completed" ]]; then
+    if [[ "$CONCLUSION" != "success" ]]; then
+      echo "Workflow failed with conclusion: $CONCLUSION"
+      exit 1
+    fi
+    exit 0
+  fi
+
+  ATTEMPT=$((ATTEMPT + 1))
+  sleep 10
+done
+
+echo "Timeout reached while waiting for workflow completion"
+exit 1
\ No newline at end of file
diff --git a/.github/workflows/sync-fork.yml b/.github/workflows/sync-fork.yml
index 639bda7..3d22140 100644
--- a/.github/workflows/sync-fork.yml
+++ b/.github/workflows/sync-fork.yml
@@ -109,6 +109,11 @@ jobs:
           fi
         id: patch_apply
 
+      - name: Push changes
+        if: steps.check_release.outputs.EXISTS == 'false' && steps.merge_attempt.outputs.MERGE_STATUS == 'clean'
+        run: |
+          git push origin ${{ env.BRANCH_NAME }}
+
       - name: Create Pull Request
         if: steps.check_release.outputs.EXISTS == 'false' && steps.merge_attempt.outputs.MERGE_STATUS == 'clean'
         uses: peter-evans/create-pull-request@v5
@@ -123,6 +128,8 @@ jobs:
           branch: ${{ env.BRANCH_NAME }}
           base: main
           labels: sync,automated
+          delete-branch: true
+          push-to-fork: ''
 
       - name: Auto-merge Pull Request
         if: |
diff --git a/.github/workflows/test_sync-fork.yml b/.github/workflows/test_sync-fork.yml
index 05f9685..b0be0e2 100644
--- a/.github/workflows/test_sync-fork.yml
+++ b/.github/workflows/test_sync-fork.yml
@@ -3,6 +3,9 @@ name: Test Sync Workflow
 on:
   workflow_dispatch:
 
+env:
+  WORKFLOW_NAME: "Sync Fork with Upstream Releases" # The name of the main workflow to be triggered
+
 jobs:
   test-sync:
     runs-on: ubuntu-latest
@@ -44,33 +47,8 @@ jobs:
         env:
           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
         run: |
-          WORKFLOW_NAME="Sync Fork with Upstream Releases"
-          TAG_NAME=${{ env.TAG_NAME }}
-
-          poll_for_workflow_run() {
-            while true; do
-              RUNS=$(gh api -H "Accept: application/vnd.github+json" \
-                /repos/${GITHUB_REPOSITORY}/actions/runs \
-                -q ".workflow_runs[] | select(.name == \"$WORKFLOW_NAME\" and .event == \"repository_dispatch\" and .head_branch == \"sync/$TAG_NAME\") | [.id, .status, .conclusion] | @csv" | head -n 1)
-              IFS=',' read -r -a RUN_DATA <<< "$RUNS"
-              if [[ -z "$RUN_DATA" ]]; then
-                continue # Continue polling if no run data found
-              fi
-              RUN_ID=${RUN_DATA[0]//\"/}
-              STATUS=${RUN_DATA[1]//\"/}
-              CONCLUSION=${RUN_DATA[2]//\"/}
-
-              if [[ "$STATUS" == "completed" ]]; then
-                echo "Workflow completed with conclusion: $CONCLUSION"
-                echo "RUN_STATUS=$STATUS" >> $GITHUB_ENV
-                echo "RUN_CONCLUSION=$CONCLUSION" >> $GITHUB_ENV
-                return
-              fi
-              sleep 10
-            done
-          }
-
-          poll_for_workflow_run
+          chmod +x .github/scripts/poll-workflow.sh
+          .github/scripts/poll-workflow.sh "${{ env.WORKFLOW_NAME }}" "${{ env.TAG_NAME }}" "${{ github.repository }}"
 
       - name: Verify main workflow success
         if: env.RUN_CONCLUSION != 'success'
